spring:
  application:
    name: htmx-demo
  jpa:
    hibernate:
      ddl-auto: create
    defer-datasource-initialization: true
  data:
    web:
      pageable:
        one-indexed-parameters: true
